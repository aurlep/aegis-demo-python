Flask==3.0.3
gunicorn==22.0.0
# Intentionally outdated, known-vulnerable dependencies so SCA scanners
# (pip-audit, osv-scanner, Trivy) have something to report. Do not "fix".
PyYAML==5.3.1
requests==2.20.0
